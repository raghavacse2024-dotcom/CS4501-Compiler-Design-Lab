# Experiment 4 — Valid Variable Recognition

Recognizes variables that start with a letter and are followed by letters or digits.

```bash
chmod +x compile.sh
./compile.sh
```

The folder also records the additional `add1` and `1add` tests shown in the manual.
