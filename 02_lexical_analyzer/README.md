# Experiment 2 — Lexical Analyzer using FLEX

## Ubuntu

```bash
chmod +x compile.sh
./compile.sh
```

The program recognizes preprocessor directives, header files, keywords, identifiers, numbers, operators and delimiters.

See `output.txt` for the manual-aligned Ubuntu terminal output.
