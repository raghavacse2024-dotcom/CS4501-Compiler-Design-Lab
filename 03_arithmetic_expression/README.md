# Experiment 3 — Arithmetic Expression Recognition

Uses FLEX and BISON to recognize arithmetic expressions containing `+`, `-`, `*`, and `/`.

```bash
chmod +x compile.sh
./compile.sh
```

The source and the two sample outcomes from the manual are included in this folder.
