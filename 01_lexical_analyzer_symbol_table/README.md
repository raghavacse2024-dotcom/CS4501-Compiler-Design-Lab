# Experiment 1 — Lexical Analyzer with Symbol Table

## Ubuntu

```bash
sudo apt update
sudo apt install -y flex bison gcc
chmod +x compile.sh
./compile.sh
```

## Input

`input.c` contains a small C-like program with identifiers, a constant, an assignment operator, an arithmetic operator, and a comment.

## Expected output

See `output.txt`. The sample output follows the manual's supplied terminal output.
