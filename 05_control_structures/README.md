# Experiment 5 — C Control Structure Syntax

Uses FLEX and BISON to recognize C-style `if`, `while`, `for`, and `switch-case` structures supported by the manual grammar.

```bash
chmod +x compile.sh
./compile.sh
```
