# Experiment 8 — Type Checking using FLEX and BISON

Builds a symbol table from declarations and checks type consistency in assignment expressions.

```bash
chmod +x compile.sh
./compile.sh
```

The manual's valid and mismatched examples are recorded in `output.txt`.
