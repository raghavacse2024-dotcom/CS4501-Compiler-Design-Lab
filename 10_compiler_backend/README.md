# Experiment 10 — Compiler Back-End

Takes three-address code statements and emits 8086-style assembly instructions using FLEX and BISON.

```bash
chmod +x compile.sh
./compile.sh
```

See `input.txt` and `output.txt` for the manual-aligned TAC and assembly output.
