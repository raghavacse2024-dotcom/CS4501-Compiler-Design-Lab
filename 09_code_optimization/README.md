# Experiment 9 — Simple Code Optimization

Implements constant folding, algebraic simplification, and strength reduction while parsing three-address-code-style assignments.

```bash
chmod +x compile.sh
./compile.sh
```

The expected terminal output follows the manual's sample output.
